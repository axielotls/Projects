# Tic Tac Toe with any size grid


def makeGrid(n):
    for row in range(n):
        for col in range(n):
            print(0, end = " ")
        print() # prints a newline after each row is filled w/ col

print(makeGrid(3))

